project(longest_substr_diff)
add_executable(${PROJECT_NAME} longest_substr_diff.cpp )

project(best_time_to_sell_stock)
add_executable(${PROJECT_NAME} best_time_to_sell_stock.cpp)

project(subarray_max_sum)
add_executable(${PROJECT_NAME} subarray_max_sum.cpp)